---
import { formatDateToKoreanString } from "../../utils/dateFormatter";

type Props = { date: Date; options?: Intl.DateTimeFormatOptions };

const { date, options } = Astro.props;
---

<time datetime={formatDateToKoreanString(date, options)}>
  {formatDateToKoreanString(date, options)}
</time>
